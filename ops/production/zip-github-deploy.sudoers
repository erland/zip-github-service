# Validate with: visudo -cf /etc/sudoers.d/zip-github-deploy
# The called script validates its sole version argument and is root-owned.
zip-github-deploy ALL=(root) NOPASSWD: /opt/zip-github/bin/deploy.sh *
