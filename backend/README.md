# zip-github backend

Clean Quarkus/Java 21 baseline. It intentionally contains no legacy verification, worker, run or artifact domain.

Run when Maven and PostgreSQL are available:

```bash
mvn test
mvn quarkus:dev
```
