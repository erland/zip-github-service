# Legacy reference

`legacy/zip-buildserver/` is a read-only source snapshot retained for migration reference. It is not part of the active build or runtime path and must not be copied wholesale back into the product.
